version: '3.8'
services:
  api_img:
    build: .
    container_name: api_img
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploaded_images:/app/uploaded_images
    restart: unless-stopped
